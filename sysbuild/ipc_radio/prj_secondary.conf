# Additional configuration for secondary device network core
# This file is used when building the secondary device to enable central role

# Enable BT_CENTRAL for secondary device to connect to primary
CONFIG_BT_CENTRAL=y
CONFIG_BT_OBSERVER=n
CONFIG_BT_MAX_CONN=6
CONFIG_BT_ATT_TX_COUNT=20

# Buffer counts to satisfy Nordic SDK 3.1.0 requirements
# Event RX count (32) must be greater than ACL TX count (30)
CONFIG_BT_BUF_EVT_RX_COUNT=32
CONFIG_BT_BUF_ACL_TX_COUNT=30

# Additional buffers for MCUmgr SMP service on secondary
CONFIG_BT_L2CAP_TX_BUF_COUNT=20
CONFIG_BT_CTLR_RX_BUFFERS=20

CONFIG_SERIAL=n
CONFIG_UART_CONSOLE=n
CONFIG_LOG=n


CONFIG_NORDIC_QSPI_NOR_FLASH_LAYOUT_PAGE_SIZE=4096