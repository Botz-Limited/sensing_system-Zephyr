# Test configuration for legacy BLE service
# This file includes the main prj.conf and enables legacy BLE

# Include the main configuration
#include "prj.conf"

# Enable legacy BLE support
CONFIG_LEGACY_BLE_ENABLED=y

# Ensure sensor data module is enabled (required for legacy BLE)
CONFIG_SENSOR_DATA_MODULE=y